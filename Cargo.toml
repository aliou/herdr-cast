[package]
name = "layout-palette"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
skim = { version = "5", default-features = false }

[profile.release]
opt-level = "z"
lto = true
strip = true
